// vite.config.ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// --- Your real Vite config below ---
export default defineConfig({
  plugins: [react()],
  // …any other options you already had…
})
